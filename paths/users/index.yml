get:
  parameters: []
  responses:
    default:
      content:
        application/msgpack:
          schema:
            type: object
            properties:
              $ref: #/definitions/UsersIndexResponse
